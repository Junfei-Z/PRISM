# PRISM Framework Dependencies
# Privacy-Aware Cloud-Edge Inference Framework

# -----------------------------------------------------------------------
# Edge SLM inference  (REQUIRED)
# -----------------------------------------------------------------------
# llama-cpp-python provides the Llama class used by EdgeDenoisingReintegrator
# to load GGUF-quantised SLMs (TinyLLaMA, Qwen1.5, StableLM-2-Zephyr, Phi-3.5).
#
# Install with CUDA GPU offloading (recommended — matches paper setup):
#   CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python --upgrade --force-reinstall --no-cache-dir
#
# Install CPU-only (no GPU required):
#   pip install llama-cpp-python
#
llama-cpp-python>=0.2.56

# -----------------------------------------------------------------------
# Core ML and numerical computing
# -----------------------------------------------------------------------
torch>=1.9.0
numpy>=1.21.0
scikit-learn>=1.0.0

# -----------------------------------------------------------------------
# Natural Language Processing
# -----------------------------------------------------------------------
spacy>=3.4.0               # NER backbone for sensitivity profiling
# After install, run: python -m spacy download en_core_web_lg

# Privacy-focused NER (Presidio)
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# -----------------------------------------------------------------------
# Cloud LLM API
# -----------------------------------------------------------------------
openai>=1.0.0              # Cloud sketch generation (GPT-4o / OpenAI-compatible)
requests>=2.25.0

# -----------------------------------------------------------------------
# Data handling and evaluation
# -----------------------------------------------------------------------
pandas>=1.3.0
openpyxl>=3.0.7            # Excel dataset files
scipy>=1.7.0
matplotlib>=3.4.0
tqdm>=4.62.0

# -----------------------------------------------------------------------
# System energy monitoring
# -----------------------------------------------------------------------
psutil>=5.9.0

# Windows-only power monitoring (uncomment if reproducing energy experiments):
# WMI>=1.5.1
# pywin32>=305
# pynvml>=11.5.0

# -----------------------------------------------------------------------
# Build tools
# -----------------------------------------------------------------------
setuptools>=65.0
wheel>=0.37.0
packaging>=21.0